# Cloudflare Pages configuration for Campus Shield
# Used by Wrangler CLI for local preview and direct deploys.
name = "campus-shield"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
